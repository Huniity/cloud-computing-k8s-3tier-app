apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: backend:latest
          imagePullPolicy: IfNotPresent

          ports:
            - containerPort: 8000
              name: http

          envFrom:
            - configMapRef:
                name: backend-config
            - secretRef:
                name: backend-secret

          # resources:
          #   requests:
          #     memory: "256Mi"
          #     cpu: "250m"
          #   limits:
          #     memory: "512Mi"
          #     cpu: "500m"

          # livenessProbe:
          #   httpGet:
          #     path: /api/
          #     port: 8000
          #   initialDelaySeconds: 30
          #   periodSeconds: 10

          # readinessProbe:
          #   httpGet:
          #     path: /api/
          #     port: 8000
          #   initialDelaySeconds: 10
          #   periodSeconds: 5